import { Fira_Code } from "next/font/google";

export const firaCode = Fira_Code({
  subsets: ["latin"],
  variable: "--font-fira-code",
  display: "swap",
});

export const CODE_FONT_FAMILY =
  'var(--font-fira-code), "Geist Mono", monospace';
