# Storage Error Handling Documentation\n\n## Overview\n\nThis document explains the comprehensive localStorage quota exceeded error handling and cleanup strategies implemented in the `CertificateMonitoringService`.\n\n## Problem Solved\n\n### ❌ Previous Implementation (Basic Error Handling)\n\n```typescript\n// OLD - Basic error handling\nprivate saveToStorage(): void {\n  try {\n    localStorage.setItem(\"ssl_monitoring_rules\", JSON.stringify(rulesObj));\n    localStorage.setItem(\"ssl_monitoring_alerts\", JSON.stringify(alertsObj));\n  } catch (error) {\n    if (error instanceof Error && error.name === \"QuotaExceededError\") {\n      console.error(\"localStorage quota exceeded. Consider cleaning up old data.\");\n      // No actual cleanup strategy implemented\n    } else {\n      console.error(\"Failed to save monitoring data to storage:\", error);\n    }\n  }\n}\n```\n\n**Issues:**\n- No automatic recovery from quota exceeded errors\n- Data loss when storage fails\n- No cleanup strategies to free up space\n- Limited error detection (only standard QuotaExceededError)\n- No retry mechanism\n\n### ✅ Current Implementation (Robust Error Handling)\n\n```typescript\n// NEW - Comprehensive error handling with cleanup and retry\nprivate saveToStorageWithRetry(retryCount: number = 0): void {\n  try {\n    // Save data\n    localStorage.setItem(\"ssl_monitoring_rules\", JSON.stringify(rulesObj));\n    localStorage.setItem(\"ssl_monitoring_alerts\", JSON.stringify(alertsObj));\n  } catch (error) {\n    if (this.isQuotaExceededError(error)) {\n      if (retryCount < maxRetries) {\n        const cleanupResult = this.performStorageCleanup(retryCount);\n        if (cleanupResult.success) {\n          // Retry after successful cleanup\n          this.saveToStorageWithRetry(retryCount + 1);\n        }\n      }\n    }\n  }\n}\n```\n\n**Improvements:**\n- ✅ Multi-browser quota error detection\n- ✅ Automatic cleanup strategies\n- ✅ Retry mechanism with progressive cleanup\n- ✅ Data preservation prioritization\n- ✅ Comprehensive error reporting and monitoring\n\n## Implementation Details\n\n### Enhanced Error Detection\n\n```typescript\nprivate isQuotaExceededError(error: unknown): boolean {\n  if (error instanceof Error) {\n    // Standard quota exceeded error\n    if (error.name === 'QuotaExceededError') return true;\n    \n    // Firefox specific error\n    if (error.name === 'NS_ERROR_DOM_QUOTA_REACHED') return true;\n    \n    // Safari specific error messages\n    if (error.message && (\n      error.message.includes('quota') ||\n      error.message.includes('storage') ||\n      error.message.includes('exceeded')\n    )) return true;\n    \n    // Chrome/Edge specific DOMException\n    if (error instanceof DOMException && error.code === 22) return true;\n  }\n  \n  return false;\n}\n```\n\n**Supports:**\n- Chrome/Edge: `QuotaExceededError`, `DOMException` code 22\n- Firefox: `NS_ERROR_DOM_QUOTA_REACHED`\n- Safari: Various quota-related error messages\n- Generic: Message content analysis\n\n### Progressive Cleanup Strategies\n\nThe system implements four levels of cleanup, applied progressively:\n\n#### 1. Resolved Alerts Cleanup (Least Aggressive)\n\n```typescript\nprivate cleanupResolvedAlerts(): { success: boolean; message: string } {\n  let removedCount = 0;\n  \n  for (const [alertId, alert] of this.alerts) {\n    if (alert.resolvedAt) {\n      this.alerts.delete(alertId);\n      removedCount++;\n    }\n  }\n  \n  return {\n    success: removedCount > 0,\n    message: `Removed ${removedCount} resolved alerts`\n  };\n}\n```\n\n**Purpose:** Remove alerts that have been resolved and no longer need monitoring\n\n#### 2. Old Alerts Cleanup\n\n```typescript\nprivate cleanupOldAlerts(maxAgeInDays: number = 30): { success: boolean; message: string } {\n  const cutoffDate = new Date(Date.now() - maxAgeInDays * 24 * 60 * 60 * 1000);\n  let removedCount = 0;\n  \n  for (const [alertId, alert] of this.alerts) {\n    const alertDate = new Date(alert.triggeredAt);\n    if (alertDate < cutoffDate) {\n      this.alerts.delete(alertId);\n      removedCount++;\n    }\n  }\n  \n  return {\n    success: removedCount > 0,\n    message: `Removed ${removedCount} alerts older than ${maxAgeInDays} days`\n  };\n}\n```\n\n**Purpose:** Remove historical alerts beyond retention period\n\n#### 3. Disabled Rules Cleanup\n\n```typescript\nprivate cleanupDisabledRules(): { success: boolean; message: string } {\n  let removedCount = 0;\n  \n  for (const [ruleId, rule] of this.monitoringRules) {\n    if (!rule.enabled) {\n      // Clear timer and remove rule\n      this.clearScheduledCheck(ruleId);\n      this.monitoringRules.delete(ruleId);\n      removedCount++;\n      \n      // Remove related alerts\n      for (const [alertId, alert] of this.alerts) {\n        if (alert.domain === rule.domain) {\n          this.alerts.delete(alertId);\n        }\n      }\n    }\n  }\n  \n  return {\n    success: removedCount > 0,\n    message: `Removed ${removedCount} disabled rules and their alerts`\n  };\n}\n```\n\n**Purpose:** Remove unused monitoring rules and their associated data\n\n#### 4. Aggressive Cleanup (Most Aggressive)\n\n```typescript\nprivate performAggressiveCleanup(): { success: boolean; message: string } {\n  const recentCutoff = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);\n  \n  // Keep only critical, recent, unresolved alerts\n  for (const [alertId, alert] of this.alerts) {\n    const alertDate = new Date(alert.triggeredAt);\n    const isCritical = alert.severity === 'critical' || alert.severity === 'high';\n    const isRecent = alertDate >= recentCutoff;\n    const isUnresolved = !alert.resolvedAt;\n    \n    if (!(isCritical && isRecent && isUnresolved)) {\n      this.alerts.delete(alertId);\n    }\n  }\n  \n  // Keep only enabled rules\n  for (const [ruleId, rule] of this.monitoringRules) {\n    if (!rule.enabled) {\n      this.clearScheduledCheck(ruleId);\n      this.monitoringRules.delete(ruleId);\n    }\n  }\n}\n```\n\n**Purpose:** Emergency cleanup keeping only essential recent data\n\n### Retry Mechanism\n\n```typescript\nprivate saveToStorageWithRetry(retryCount: number = 0): void {\n  const maxRetries = 3;\n  \n  try {\n    // Attempt to save\n    localStorage.setItem(\"ssl_monitoring_rules\", JSON.stringify(rulesObj));\n    localStorage.setItem(\"ssl_monitoring_alerts\", JSON.stringify(alertsObj));\n    \n  } catch (error) {\n    if (this.isQuotaExceededError(error) && retryCount < maxRetries) {\n      const cleanupResult = this.performStorageCleanup(retryCount);\n      \n      if (cleanupResult.success) {\n        // Retry with delay\n        setTimeout(() => {\n          this.saveToStorageWithRetry(retryCount + 1);\n        }, 100);\n      }\n    }\n  }\n}\n```\n\n**Features:**\n- Progressive cleanup strategies based on retry attempt\n- Maximum retry limit to prevent infinite loops\n- Delay between retries for cleanup completion\n- Success tracking and logging\n\n### Comprehensive Error Reporting\n\n```typescript\nprivate handleStorageFailure(error: unknown, context: string): void {\n  const errorInfo = {\n    context,\n    timestamp: new Date().toISOString(),\n    error: error instanceof Error ? {\n      name: error.name,\n      message: error.message,\n      stack: error.stack\n    } : error,\n    storageInfo: this.getStorageInfo(),\n    dataInfo: {\n      rulesCount: this.monitoringRules.size,\n      alertsCount: this.alerts.size,\n      activeTimersCount: this.activeTimers.size\n    }\n  };\n  \n  // Log to console\n  console.error('🚨 Storage operation failed:', errorInfo);\n  \n  // Save to sessionStorage as backup\n  try {\n    const errorLog = JSON.parse(sessionStorage.getItem('ssl_monitoring_errors') || '[]');\n    errorLog.push(errorInfo);\n    sessionStorage.setItem('ssl_monitoring_errors', JSON.stringify(errorLog));\n  } catch {}\n  \n  // Emit custom event for application handling\n  window.dispatchEvent(new CustomEvent('ssl-monitoring-storage-error', {\n    detail: errorInfo\n  }));\n}\n```\n\n**Provides:**\n- Detailed error context and metadata\n- Storage usage information\n- Data state at time of error\n- Backup error logging to sessionStorage\n- Custom events for application-level handling\n\n## Usage Examples\n\n### Basic Error Monitoring\n\n```typescript\n// Listen for storage errors\nwindow.addEventListener('ssl-monitoring-storage-error', (event) => {\n  const errorInfo = event.detail;\n  \n  switch (errorInfo.context) {\n    case 'quota_exceeded_cleanup_failed':\n      // Handle cleanup failure\n      break;\n    case 'quota_exceeded_max_retries':\n      // Handle max retries exceeded\n      break;\n    case 'storage_error':\n      // Handle general storage errors\n      break;\n  }\n});\n```\n\n### Manual Cleanup\n\n```typescript\nconst monitor = CertificateMonitoringService.getInstance();\n\n// Gentle cleanup\nconst result = monitor.cleanupStorage(false);\nconsole.log('Cleanup result:', result);\n\n// Aggressive cleanup\nconst aggressiveResult = monitor.cleanupStorage(true);\nconsole.log('Aggressive cleanup:', aggressiveResult);\n```\n\n### Storage Health Monitoring\n\n```typescript\n// Monitor storage usage\nsetInterval(() => {\n  const rules = monitor.getMonitoringRules();\n  const alerts = monitor.getAlerts();\n  const totalSize = JSON.stringify({rules, alerts}).length;\n  \n  if (totalSize > 500 * 1024) { // 500KB\n    console.warn('Storage usage high - consider cleanup');\n    monitor.cleanupStorage(false);\n  }\n}, 60000); // Check every minute\n```\n\n## Error Contexts and Handling\n\n### Error Context Types\n\n| Context | Description | Recommended Action |\n|---------|-------------|--------------------|\n| `quota_exceeded_cleanup_failed` | Quota exceeded and cleanup failed | Manual intervention needed |\n| `quota_exceeded_max_retries` | Max retry attempts exceeded | Reduce data or increase cleanup frequency |\n| `storage_error` | General storage error | Check browser storage settings |\n\n### Response Strategies\n\n#### For `quota_exceeded_cleanup_failed`\n\n```typescript\nif (errorInfo.context === 'quota_exceeded_cleanup_failed') {\n  // Immediate actions:\n  // 1. Notify user of storage issues\n  // 2. Offer manual cleanup options\n  // 3. Consider exporting data before cleanup\n  \n  const exportData = monitor.exportData();\n  // Save or offer download of exported data\n  \n  // Perform aggressive cleanup\n  monitor.cleanupStorage(true);\n}\n```\n\n#### For `quota_exceeded_max_retries`\n\n```typescript\nif (errorInfo.context === 'quota_exceeded_max_retries') {\n  // Strategic actions:\n  // 1. Analyze storage usage patterns\n  // 2. Increase cleanup frequency\n  // 3. Reduce data retention periods\n  \n  // Set up more frequent cleanup\n  setInterval(() => {\n    monitor.cleanupStorage(false);\n  }, 30 * 60 * 1000); // Every 30 minutes\n}\n```\n\n## Storage Information and Monitoring\n\n### Storage Usage Analysis\n\n```typescript\nprivate getStorageInfo(): Record<string, unknown> {\n  const storageInfo: Record<string, unknown> = {\n    userAgent: navigator.userAgent,\n    localStorageLength: localStorage.length,\n    sessionStorageLength: sessionStorage?.length || 0\n  };\n  \n  // Calculate approximate localStorage usage\n  let totalSize = 0;\n  for (const key in localStorage) {\n    if (localStorage.hasOwnProperty(key)) {\n      totalSize += localStorage[key].length + key.length;\n    }\n  }\n  storageInfo.approximateSize = totalSize;\n  \n  // Add storage quota information if available\n  if (navigator.storage?.estimate) {\n    navigator.storage.estimate().then((quota) => {\n      if (quota.quota && quota.usage) {\n        storageInfo.quota = quota.quota;\n        storageInfo.usage = quota.usage;\n        storageInfo.usagePercentage = ((quota.usage / quota.quota) * 100).toFixed(2);\n      }\n    });\n  }\n  \n  return storageInfo;\n}\n```\n\n### Health Monitoring Dashboard\n\n```typescript\ninterface StorageHealth {\n  status: 'healthy' | 'warning' | 'critical';\n  usage: {\n    rules: { count: number; size: number };\n    alerts: { count: number; size: number };\n    total: { size: number; percentage?: number };\n  };\n  recommendations: string[];\n  lastCleanup?: string;\n}\n\nfunction getStorageHealth(): StorageHealth {\n  const monitor = CertificateMonitoringService.getInstance();\n  const rules = monitor.getMonitoringRules();\n  const alerts = monitor.getAlerts();\n  \n  const rulesSize = JSON.stringify(rules).length;\n  const alertsSize = JSON.stringify(alerts).length;\n  const totalSize = rulesSize + alertsSize;\n  \n  const recommendations: string[] = [];\n  let status: StorageHealth['status'] = 'healthy';\n  \n  // Analyze and provide recommendations\n  if (totalSize > 1024 * 1024) { // 1MB\n    status = 'critical';\n    recommendations.push('Immediate cleanup required');\n  } else if (totalSize > 500 * 1024) { // 500KB\n    status = 'warning';\n    recommendations.push('Consider cleanup soon');\n  }\n  \n  const disabledRules = rules.filter(r => !r.enabled);\n  if (disabledRules.length > 5) {\n    recommendations.push(`Remove ${disabledRules.length} disabled rules`);\n  }\n  \n  const oldAlerts = alerts.filter(alert => {\n    const age = Date.now() - new Date(alert.triggeredAt).getTime();\n    return age > 30 * 24 * 60 * 60 * 1000; // 30 days\n  });\n  \n  if (oldAlerts.length > 10) {\n    recommendations.push(`Clean up ${oldAlerts.length} old alerts`);\n  }\n  \n  return {\n    status,\n    usage: {\n      rules: { count: rules.length, size: rulesSize },\n      alerts: { count: alerts.length, size: alertsSize },\n      total: { size: totalSize }\n    },\n    recommendations\n  };\n}\n```\n\n## Best Practices\n\n### 1. Proactive Monitoring\n\n```typescript\n// Set up regular health checks\nsetInterval(() => {\n  const health = getStorageHealth();\n  \n  if (health.status === 'warning') {\n    monitor.cleanupStorage(false); // Gentle cleanup\n  } else if (health.status === 'critical') {\n    monitor.cleanupStorage(true); // Aggressive cleanup\n  }\n}, 5 * 60 * 1000); // Every 5 minutes\n```\n\n### 2. Data Retention Policies\n\n```typescript\n// Implement data retention policies\nconst RETENTION_POLICIES = {\n  resolvedAlerts: 7 * 24 * 60 * 60 * 1000,    // 7 days\n  generalAlerts: 30 * 24 * 60 * 60 * 1000,    // 30 days\n  criticalAlerts: 90 * 24 * 60 * 60 * 1000,   // 90 days\n  disabledRules: 0 // Remove immediately\n};\n\nfunction applyRetentionPolicy() {\n  const monitor = CertificateMonitoringService.getInstance();\n  \n  // Apply policies based on data type and criticality\n  monitor.getAlerts().forEach(alert => {\n    const age = Date.now() - new Date(alert.triggeredAt).getTime();\n    let maxAge = RETENTION_POLICIES.generalAlerts;\n    \n    if (alert.resolvedAt) {\n      maxAge = RETENTION_POLICIES.resolvedAlerts;\n    } else if (alert.severity === 'critical') {\n      maxAge = RETENTION_POLICIES.criticalAlerts;\n    }\n    \n    if (age > maxAge) {\n      monitor.deleteAlert(alert.id);\n    }\n  });\n}\n```\n\n### 3. Error Recovery Planning\n\n```typescript\n// Implement error recovery plan\nclass StorageErrorRecovery {\n  private errorCount = 0;\n  private lastError?: Date;\n  \n  handleError(errorInfo: any) {\n    this.errorCount++;\n    this.lastError = new Date();\n    \n    // Escalate based on error frequency\n    if (this.errorCount > 5) {\n      // Multiple errors - aggressive action needed\n      this.performEmergencyCleanup();\n    } else if (this.errorCount > 2) {\n      // Some errors - proactive cleanup\n      this.performPreventiveCleanup();\n    }\n    \n    // Reset counter after 1 hour\n    setTimeout(() => {\n      this.errorCount = Math.max(0, this.errorCount - 1);\n    }, 60 * 60 * 1000);\n  }\n  \n  private performEmergencyCleanup() {\n    const monitor = CertificateMonitoringService.getInstance();\n    \n    // Export critical data first\n    const criticalData = this.extractCriticalData();\n    \n    // Perform aggressive cleanup\n    monitor.cleanupStorage(true);\n    \n    // Restore only critical data\n    this.restoreCriticalData(criticalData);\n  }\n}\n```\n\n## Performance Considerations\n\n### Cleanup Performance\n\n- **Resolved alerts cleanup**: O(n) where n = number of alerts\n- **Old alerts cleanup**: O(n) with date comparison overhead\n- **Disabled rules cleanup**: O(n × m) where n = rules, m = alerts per rule\n- **Aggressive cleanup**: O(n + m) with filtering logic\n\n### Memory Usage\n\n- Cleanup operations are performed in-place to minimize memory overhead\n- Large data exports are avoided during emergency cleanup\n- Retry delays prevent memory pressure from rapid retries\n\n### Storage Impact\n\n- Progressive cleanup ensures minimal data loss\n- Critical data preservation during aggressive cleanup\n- Efficient JSON serialization for storage operations\n\n## Testing\n\n### Unit Tests\n\n```typescript\ndescribe('Storage Error Handling', () => {\n  it('should detect quota exceeded errors', () => {\n    const quotaError = new DOMException('QuotaExceededError');\n    quotaError.name = 'QuotaExceededError';\n    \n    expect(service.isQuotaExceededError(quotaError)).toBe(true);\n  });\n  \n  it('should perform cleanup and retry', async () => {\n    const service = new CertificateMonitoringService();\n    \n    // Mock localStorage to throw quota error\n    const setItemSpy = jest.spyOn(Storage.prototype, 'setItem')\n      .mockImplementationOnce(() => {\n        throw new DOMException('QuotaExceededError');\n      })\n      .mockImplementationOnce(() => {}); // Success on retry\n    \n    await service.saveToStorage();\n    \n    expect(setItemSpy).toHaveBeenCalledTimes(2);\n  });\n});\n```\n\n### Integration Tests\n\n```typescript\n// Test full error recovery flow\nit('should recover from quota exceeded with cleanup', async () => {\n  // Fill storage near capacity\n  const monitor = CertificateMonitoringService.getInstance();\n  \n  // Add many test rules and alerts\n  for (let i = 0; i < 1000; i++) {\n    monitor.addMonitoringRule(testRule);\n    // Add test alerts\n  }\n  \n  // Trigger quota exceeded\n  const largData = 'x'.repeat(5 * 1024 * 1024); // 5MB\n  try {\n    localStorage.setItem('large_test_data', largeData);\n  } catch {}\n  \n  // Attempt save - should trigger cleanup and recovery\n  await monitor.saveToStorage();\n  \n  // Verify data was saved successfully\n  const savedRules = JSON.parse(localStorage.getItem('ssl_monitoring_rules'));\n  expect(savedRules).toBeDefined();\n  \n  // Cleanup\n  localStorage.removeItem('large_test_data');\n});\n```\n\n## Migration Guide\n\nThe improved error handling is backward compatible - no changes needed for existing code. New features available:\n\n### New Methods\n\n```typescript\n// Manual cleanup\nmonitor.cleanupStorage(aggressive: boolean): CleanupResult\n\n// Error event listening\nwindow.addEventListener('ssl-monitoring-storage-error', handler);\n\n// Storage health monitoring\nconst health = getStorageHealth();\n```\n\n### Enhanced Error Information\n\nError events now include:\n- Detailed error context\n- Storage usage information\n- Data state metadata\n- Recovery recommendations\n\nThis implementation provides robust, production-ready storage error handling with automatic recovery capabilities and comprehensive monitoring.